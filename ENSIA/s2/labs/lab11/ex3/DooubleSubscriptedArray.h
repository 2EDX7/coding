#include <iostream>

template<typename t>


class DoubleSubscriptedArray{
    public:
        operator(int,int);
        operator=;
        operator==;
        operator!=;
        operator>>;
        operator<<;
    private:
        int rowSize;
        int ColumnSize;
        int* ptr;
};